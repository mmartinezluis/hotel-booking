<div class="view">
  <h1>Cities Visited</h1>
  
  <%= render partial: 'messages/msg' %>

  <ul>
    <% @cities.each do |city| %>
      <li>
        <%= link_to city.code, city_hotels_path(city.id) %>
      </li>
    <% end %>
  </ul>
</div>
    