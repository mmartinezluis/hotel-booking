<div class="view">
  <h1>Cities Visited</h1>
  
  <%= render partial: 'messages/msg' %>

  <% if @user.cities.blank? %>
    <p>You haven't visited any cities yet<p>
  <% else %>
    <ul>
      <% @user.cities.distinct.each do |city| %>
        <li>
          <%= link_to city.code, city_hotels_path(city.id) %>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>
    